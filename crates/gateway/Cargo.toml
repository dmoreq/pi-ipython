[package]
name = "gateway"
version = "0.1.0"
edition = "2021"
description = "Jupyter kernel gateway process lifecycle management"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = "3"
